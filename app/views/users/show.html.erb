<table class="profile" summary="Profile info">
  <tr>
	<td class="main">
           <h1>
 		 <%= gravatar_image_tag @user.email, :class => "gravatar"%>
  		<%= @user.name %>
          </h1>
	</td>

	<td class="sidebar round">
	  <strong>Name</strong> <%= @user.name %> <br />
	  <strong>Profile</strong> <%= link_to user_path(@user), @user %>
	</td>

  </tr>
</table>
